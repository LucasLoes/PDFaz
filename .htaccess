RewriteEngine On

# Redirecionar / mapear URLs amigáveis sem .html para o respectivo arquivo .html no Apache
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html [L]
